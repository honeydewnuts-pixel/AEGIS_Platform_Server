# android-app

The production Android client lives in **`../mobile_app/`** (Gradle / Kotlin).

This folder exists for monorepo clarity (`v1.0.0-monorepo`). Build:

```bash
cd ../mobile_app
./gradlew assembleDebug
```

APK artifacts: GitHub Actions Mobile CI + website `/downloads/aegis-mobile.apk`.
