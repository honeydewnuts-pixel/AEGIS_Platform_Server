package com.aegis.mobile.ui

/**
 * Legacy activity name kept so any residual intent/component still resolves.
 * Indicator templates are retired — this opens the pairs/rulebook registry UI.
 */
class IndicatorSetupActivity : RegistryActivity()
