# Copy to mobile_app/keystore.properties (gitignored) for local release builds.
# Do NOT commit keystore.properties or *.jks / *.keystore files.

storeFile=../secrets/aegis-release.jks
storePassword=REPLACE_ME
keyAlias=aegis
keyPassword=REPLACE_ME
