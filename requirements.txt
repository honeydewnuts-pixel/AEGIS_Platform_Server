# Pinned versions. These reflect what was current/stable as of this
# codebase's development (early 2026) - they have NOT been checked
# against live PyPI from this environment (no network access here).
# Run `pip list --outdated` and `pip-audit` (see .github/workflows/
# backend-ci.yml) for real, up-to-date vulnerability info before
# deploying - pinning is a starting point for reproducibility, not a
# substitute for actually scanning.
#
# fastapi/starlette note (fixed after a real pip-audit CI failure):
# FastAPI does NOT pin an upper bound on starlette - its own metadata
# only requires starlette>=0.46.0, so bumping fastapi alone does not
# guarantee a patched starlette. The line below pins starlette
# explicitly, above every CVE pip-audit found in 0.48.0 (host header
# injection enabling auth bypass, path-based SSRF, a Range-header ReDoS,
# and an HTTPEndpoint method-dispatch issue - fixed across versions up
# to 1.3.1). fastapi is bumped to a version built for the lifespan API
# (main.py no longer uses @app.on_event, which Starlette 1.0+ removed).

fastapi>=0.136.3
starlette>=1.3.1
uvicorn[standard]==0.34.0
python-multipart==0.0.20
opencv-python-headless==4.10.0.84
numpy==1.26.4
pillow==11.0.0
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
python-dotenv==1.0.1
requests==2.32.3
aiofiles==24.1.0
cryptography==44.0.0
redis==5.2.1
httpx==0.28.1
stripe==11.4.1
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
prometheus-fastapi-instrumentator
slowapi
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-grpc
opentelemetry-instrumentation-fastapi
opentelemetry-instrumentation-sqlalchemy
opentelemetry-instrumentation-redis
opentelemetry-instrumentation-httpx

# Windows-only, and requires the MT5 desktop terminal installed on the
# same machine. Only install this in the worker-pool image/VM, not in
# the main API image (see requirements-worker.txt).
# MetaTrader5
